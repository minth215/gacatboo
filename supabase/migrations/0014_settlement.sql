-- =====================================================================
-- 0014: 정산 그룹 지원 — 멤버별 정산 금액 수동 조정값 추가
--   · group_members.settlement_override — "정산" 탭에서 총무가 직접 지정한
--     멤버별 정산 금액(원). null 이면 결제 총액을 멤버 수로 나눈 기본값 사용.
-- 0001~0013 이후 실행. 여러 번 실행해도 안전.
-- =====================================================================

alter table public.group_members add column if not exists settlement_override bigint;
