-- =====================================================================
-- 0012: 그룹 카드 배경색 설정 추가
--   · groups.color — 그룹 아이콘 타일 배경색(hex). 그룹 생성/수정 시 직접 지정.
--   · 그룹의 이모지(category_emoji)는 이제 카테고리와 무관하게 그룹마다 독립적으로
--     지정합니다(그룹 카테고리 관리에서는 이름만 관리).
-- 0001~0011 이후 실행. 여러 번 실행해도 안전.
-- =====================================================================

alter table public.groups add column if not exists color text not null default '';
