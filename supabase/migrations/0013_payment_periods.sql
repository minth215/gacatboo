-- =====================================================================
-- 0013: 결제 내역(subscription_payments)에 기간(회차) 컬럼 추가
--   · 결제 1건이 여러 결제 주기분을 한 번에 커버(선결제 등)할 때 사용하며,
--     통계 탭의 "다음 결제일" 계산에 반영됩니다. 기본값 1.
-- 0001~0012 이후 실행. 여러 번 실행해도 안전.
-- =====================================================================

alter table public.subscription_payments add column if not exists periods int not null default 1;
