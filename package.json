{
  "name": "gacatboo",
  "version": "2.0.0",
  "private": true,
  "description": "가계부 웹 애플리케이션 (Household Ledger) — Supabase 백엔드",
  "scripts": {
    "install:all": "npm --prefix client install",
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "preview": "npm --prefix client run preview"
  }
}
